# Database Configuration
# For PostgreSQL (required for production, optional for development with in-memory fallback)
DATABASE_URL=postgresql://user:password@localhost:5432/runescapeapp

# Session Configuration
# Generate a secure random string for production
SESSION_SECRET=change-this-to-a-secure-random-string-in-production

# OpenAI Configuration (optional)
# If not provided, the app will use mock AI responses for script generation
OPENAI_API_KEY=sk-your-openai-api-key-here

# Node Environment
NODE_ENV=development

# Port Configuration
PORT=5000
